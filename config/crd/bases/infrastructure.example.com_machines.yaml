---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: machines.infrastructure.example.com
spec:
  group: infrastructure.example.com
  names:
    kind: Machine
    listKind: MachineList
    plural: machines
    singular: machine
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Machine is the Schema for the machines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Machine
            properties:
              flavor:
                description: Flavor specifies the compute size of the machine.
                minLength: 1
                type: string
              image:
                description: Image specifies the operating system image used for the
                  machine.
                minLength: 1
                type: string
              region:
                description: Region specifies where the machine should be provisioned.
                minLength: 1
                type: string
            required:
            - flavor
            - image
            - region
            type: object
          status:
            description: status defines the observed state of Machine
            properties:
              providerID:
                description: ProviderID is the unique identifier assigned by the infrastructure
                  provider.
                type: string
              state:
                description: State represents the current state reported by the infrastructure
                  provider.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
